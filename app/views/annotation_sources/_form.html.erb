<%= form_for(@annotation_source) do |f| %>
  <% if @annotation_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@annotation_source.errors.count, "error") %> prohibited this annotation_source from being saved:</h2>

      <ul>
      <% @annotation_source.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
